import { OptionPipePipe } from './option-pipe.pipe';

describe('OptionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OptionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
